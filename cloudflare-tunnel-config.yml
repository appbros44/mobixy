tunnel: mobixy-proxy
credentials-file: /etc/cloudflared/mobixy-proxy.json

ingress:
  # Handle HTTP proxy requests for proxy.apkroute.com
  - hostname: proxy.apkroute.com
    service: http://127.0.0.1:8787
    # Disable SSL verification and allow proxy headers
    originRequest:
      proxyPort: 8787
      connectTimeout: 30s
      noTLSVerify: true
  
  # Handle regular HTTP/HTTPS for api.apkroute.com
  - hostname: api.apkroute.com
    service: http://127.0.0.1:8787
  
  # Default catch-all
  - service: http_status:404
